---
type: session
slug: deploy-project-state-2026-06-22
reviewed: true
---
# Deploy do Project State em produção

## Resumo

Schema de Project State aplicado em produção e API validada com healthcheck público.

Resumo revisado da sessão de deploy do MyInst Project State.

- Schema aplicado em produção via container da API com drizzle-kit push.
- Backup pré-schema criado na VPS antes da alteração.
- API validada em produção com healthcheck público retornando 200.
- Rota de Project State validada como existente e protegida por autenticação.
- Frontend validado com bundle contendo as abas de Project State.

Conteúdo revisado e mantido apenas com informações operacionais não sensíveis.
